# Instrucoes
- Sempre aplique i18n em textos de interface, mensagens e labels.
- Use `src/lib/i18n.ts` para adicionar/atualizar mensagens em `pt-BR` e `en-US`.
- Evite strings hardcoded na UI; prefira `t("...")` com chaves existentes.
