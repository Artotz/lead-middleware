import { DEFAULT_LOCALE } from "@/lib/i18n";
import ActionsListClient from "./actions-list-client";

export default function CronogramaAcoesPage() {
  return <ActionsListClient locale={DEFAULT_LOCALE} />;
}
